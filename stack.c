#include "monty.h"
/**
 * _stack - function prints the top
 * @head: stack
 * written by apete
 * @counter: lne_number
 * Return: never return
*/
void _stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	opc.sq_flag = 0;
}
